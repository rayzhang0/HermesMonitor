# Hermes Monitor

This repository contains both parts of the Hermes Monitor project:

- `hermes-monitor/` contains the Python monitoring service and public JSON feed generator.
- `hermes-ios/` contains the SwiftUI iPhone and iPad app.
